// Centralized version configuration
export const APP_VERSION = 'v1.2.0'
export const VERSION_SUFFIX = 'beta'
export const IS_BETA = true
export const FULL_VERSION = `${APP_VERSION}-${VERSION_SUFFIX}`
